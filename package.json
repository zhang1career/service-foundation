{
  "name": "service-foundation",
  "private": true,
  "scripts": {
    "build:nvl": "node build-nvl.js",
    "build:tailwind": "tailwindcss -i ./app_console/frontend/tailwind-input.css -o ./app_console/static/console/css/tailwind.css --minify",
    "build": "npm run build:tailwind && npm run build:nvl"
  },
  "dependencies": {
    "@neo4j-nvl/base": "1.1.0",
    "@neo4j-nvl/interaction-handlers": "1.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "tailwindcss": "^3.4.0"
  }
}
